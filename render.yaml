services:
  - type: web
    name: soderia
    env: docker
    dockerfilePath: Dockerfile.postgres
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: soderia
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: PORT
        value: "3000"
